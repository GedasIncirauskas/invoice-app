import React from "react";
import styles from "./${name}.module.scss";

const ${name} = (props) => {
    return (
      <div >
        ${name}
      </div>
    );
};

${name}.propTypes = {};

export default ${name};
